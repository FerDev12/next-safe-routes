---
title: Quick Start
description: Get up and running with Next Safe Routes in your project.
---

<Note title='Supported Versions' type='info'>
  Next Safe Routes currently only supports Next.js versions >= 13.0.0 using the
  "app" router.
</Note>

## Guide

<Tabs defaultValue='npm'>
<Stepper>
<StepperItem title='Create Next App'>
Create a Next.js app with typescript if you don't already have one
    
  <TabsList>
    <TabsTrigger value='npm'>npm</TabsTrigger>
    <TabsTrigger value='pnpm'>pnpm</TabsTrigger>
    <TabsTrigger value='yarn'>yarn</TabsTrigger>
  </TabsList>
  <TabsContent value='npm'>
    ```bash
    npx create-next-app@latest --typescript 
    ```
  </TabsContent>
  <TabsContent value='pnpm'>
    ```bash 
    pnpm create next-app@latest --typescript
    ```
  </TabsContent>
  <TabsContent value='yarn'>
    ```bash 
    yarn create next-app@latest --typescript
    ```
  </TabsContent>

</StepperItem>

<StepperItem title='Install Next Safe Routes'>

  <TabsList>
    <TabsTrigger value='npm'>npm</TabsTrigger>
    <TabsTrigger value='pnpm'>pnpm</TabsTrigger>
    <TabsTrigger value='yarn'>yarn</TabsTrigger>
  </TabsList>
  <TabsContent value='npm'>

    ```bash
    npm install next-safe-routs
    ```

  </TabsContent>
  <TabsContent value='pnpm'>

    ```bash
    pnpm add next-safe-routes
    ```

  </TabsContent>
  <TabsContent value='yarn'>

    ```bash
    yarn add next-safe-routes
    ```

  </TabsContent>

</StepperItem>

<StepperItem title='Generate Routes Type'>
This will generate a file under src/types/routes.ts

<Tabs defaultValue='plugin' >
  <TabsList>
    <TabsTrigger value='plugin'>Plugin</TabsTrigger>
    <TabsTrigger value='cli'>CLI</TabsTrigger>
  </TabsList>
  <TabsContent value='plugin'>

    ```js
    // next.config.mjs

    import { withNextSafeRoutes } from 'next-safe-routes/plugin';

    /** @type {import('next').NextConfig} */
    const nextConfig = withNextSafeRoutes({
      // The rest of your next.config.
    });

    export default nextConfig;
    ```

<Tabs defaultValue='npm'>
  <TabsList>
    <TabsTrigger value='npm'>npm</TabsTrigger>
    <TabsTrigger value='pnpm'>pnpm</TabsTrigger>
    <TabsTrigger value='yarn'>yarn</TabsTrigger>
  </TabsList>
  <TabsContent value='npm'>

    ```bash
    npm run build
    # or
    npm run dev
    ```

  </TabsContent>
  <TabsContent value='pnpm'>

    ```bash
    pnpm build
    # or
    pnpm dev
    ```

  </TabsContent>
  <TabsContent value='yarn'>

    ```bash
    yarn build
    # or
    yarn dev
    ```

  </TabsContent>
</Tabs>

{' '}

<Note title='Turbopack Support' type='warning'>
  Next Safe Routes currently does not support the use of turbopack.
</Note>

  </TabsContent>
  <TabsContent value='cli'>
    ```bash 
    npm run generate-routes
    ```

  </TabsContent>
</Tabs>

</StepperItem>

<StepperItem title='Create Navigation Utils'>
_We recommend creating a navigation.ts file at the root of your src directory_

```ts
// navigation.ts

import { createNextSafeNavigation } from 'next-safe-routes';
import { Routes } from './types/routes';

export const { getRoute, Link, useRouter, redirect } =
  createNextSafeNavigation<Routes>();
```

</StepperItem>
</Stepper>
</Tabs>

## Example Usage

### getRoute

![getRoute example GIF](/get-route-example.gif)

### useRouter

![useRouter example GIF](/use-router-example.gif)

### redirect

![redirect example GIF](/redirect-example.gif)

### \<Link>

![link example GIF](/link-example.gif)
